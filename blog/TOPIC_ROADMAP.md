# Topic Roadmap

이 문서는 블로그를 `글 수 확장`보다 `사람에게 바로 도움이 되는 검색 허브`로 키우기 위한 운영 문서다.

현재 기준은 워크트리 기준 34개 포스트다.

## 지금 상태 요약

강한 축:

- DART / EDGAR 전체 구조
- 사업보고서 읽기 기본 흐름
- 대주주 / 특수관계인 / 임원 보수 / 주주환원 / 주총
- 생산능력 / 건설중인자산 / 감가상각
- 매출채권 / 재고 / 이익의 질
- OpenDART / XBRL / 원문 다운로드 파이프라인

약한 축:

- 초보자 입문 허브 글의 두께
- 시리즈별 `다음 글로 이동시키는` 연결성
- 본문 안 문맥형 내부 링크
- 글마다 비슷하게 반복되는 템플릿
- SVG의 정보 밀도와 다양성
- 기업 이벤트·자금조달 공시 시리즈의 후속 확장 부족

## 전수 점검 결과

`scripts/auditBlog.py` 기준으로 지금 가장 중요한 수치는 아래다.

- 포스트 수: `34`
- 1,200단어 미만 포스트: `0`
- SVG 4개 미만 포스트: `0`
- FAQ 없는 포스트: `0`
- 체크리스트성 헤딩이 없는 포스트: `0`
- XML 파서 기준 깨진 SVG: `0`
- 텍스트 노드 3개 이하 저밀도 SVG: `0`
- 가장 많이 반복된 H2:
  - `FAQ`
  - `정리`
  - `참고한 공식 자료`
  - `10분 체크리스트`

이 수치가 말하는 핵심은 단순하다.

- 글이 너무 적은 것이 아니다.
- `기초 허브 글`, `시리즈 구조`, `SVG 체계`가 아직 균일하지 않다.

## 운영 방향 전환

이제부터는 `새 글 1개`보다 `기존 허브 글 1개 강화`를 더 높은 우선순위로 둔다.

### 우선순위 1

- 검색형 입문 글 강화
- 시리즈 첫 글 강화
- SVG 깨짐과 낮은 정보 밀도 해결

### 우선순위 2

- 비어 있는 시리즈를 실전 검색 의도로 채우기
- 문맥형 내부 링크를 늘려 카테고리 허브를 강하게 만들기

### 우선순위 3

- 새 주제를 확장하되, 기존 글과 겹치지 않는 질문만 추가

## 사람 도움 중심 개편 원칙

- 너무 깊게 들어가기보다 `지금 무엇을 봐야 하나`를 먼저 답한다.
- 입문 글은 정의보다 `첫 행동`을 먼저 준다.
- 판단 글은 공식 설명보다 `좋은 경우 vs 위험한 경우`를 먼저 보여준다.
- 파이프라인 글은 기능 나열보다 `무엇을 어떤 순서로 붙여야 하는지`를 먼저 준다.
- 같은 시리즈라도 구조를 똑같이 복제하지 않는다.

## AI 검색 노출 원칙

공식 가이드 기준으로 중요한 것은 새 AI 전용 문법이 아니라 `사람 기준의 유용한 본문`이다.

- 첫 문단에서 질문에 직접 답한다.
- 본문 안에 정의, 판단 기준, 다음 행동이 같이 있어야 한다.
- robots, sitemap, JSON-LD는 계속 유지하되, 그것만으로 해결된다고 보지 않는다.
- OAI-SearchBot, Google-Extended, ClaudeBot, PerplexityBot 허용 상태를 유지한다.
- 본문 링크가 시리즈 구조를 실제로 설명해야 한다.

## 시리즈 재정렬

시리즈 id는 유지하되, 사용자에게 보이는 포지션은 아래처럼 정리한다.

| 시리즈 id | 사용자용 포지션 | 현재 상태 |
| --- | --- | --- |
| `dart-foundations` | DART 첫걸음 | 핵심 입문 허브 보강 완료, 유지 단계 |
| `edgar-reading` | EDGAR 실전 입문 | 핵심 허브 보강 완료, 유지 단계 |
| `report-reading-foundations` | 사업보고서 실전 읽기 | 핵심 허브 보강 완료, 유지 단계 |
| `audit-and-governance-reading` | 감사와 경고 신호 | 핵심 허브 보강 완료, 유지 단계 진입 |
| `ownership-and-governance-reading` | 대주주·보수·주주환원 | 핵심 허브 보강 완료, 유지 단계 진입 |
| `fixed-cost-and-capex` | 설비투자와 고정비 | 좋은 축, 유지 단계 |
| `financial-context` | 숫자 뒤 맥락 읽기 | 핵심 허브 보강 완료, 확장 글 필요 |
| `working-capital-and-earnings-quality` | 재고·채권·이익의 질 | 핵심 허브 보강 완료, 현금흐름 글 필요 |
| `data-automation` | 공시 데이터 파이프라인 | 핵심 파이프라인 허브 보강 완료, 유지 단계 |
| `corporate-actions-and-financing` | 이벤트·자금조달 공시 | `032`, `033` 완료, 자기주식/합병 쪽 확장 필요 |

## 리라이트 우선순위

### A. 가장 먼저 손볼 글

1. `004-reading-business-reports` `완료`
2. `021-where-to-click-first-in-dart` `완료`
3. `009-edgar-filing-original-text-api` `완료`

이 세 글은 2026-03-18 기준 1차 리라이트를 끝냈다. 다음 배치는 오너십 클러스터와 남은 짧은 허브 글로 넘어간다.

### B. 시리즈 묶음 리라이트

1. `022-major-shareholder-and-related-parties` `완료`
2. `023-executive-pay-disclosure` `완료`
3. `024-shareholder-return-what-matters` `완료`
4. `025-governance-red-flags` `완료`
5. `026-how-to-read-agm-notice` `완료`
6. `027-good-vs-risky-ownership` `완료`

이 묶음은 2026-03-18 기준 1차 리라이트를 마쳤다. 오너십 시리즈는 이제 `유지/정교화` 단계로 넘어간다.

### C. SVG 우선 보강 대상

1. 텍스트 노드 3개 이하 자산 3개 교체 `완료`
2. 첫 SVG가 질문을 설명하지 못하는 입문 글 재검토
3. 입문 허브 글의 첫 SVG를 질문-답변형으로 재정비

### D. 남은 짧은 허브 리라이트

1. `007-edgar-integrated-playbook` `완료`
2. `008-business-section-changes-judgment` `완료`
3. `018-why-rising-sales-can-still-be-risky` `완료`
4. `016-opendart-material-events` `완료`
5. `028-opendart-xbrl-notes-pipeline` `완료`

남은 `1,200단어 미만` 포스트는 이제 `0`개다. 다음 우선순위는 리라이트보다 신규 시리즈 생산이다.

### E. 구조 정규화 현황

1. `001/002/003/005/006` FAQ·체크리스트 헤딩 정규화 `완료`
2. FAQ 누락 `0`
3. 체크리스트성 헤딩 누락 `0`

## 다음 신규 글 우선순위

### 1. 영업현금흐름이 순이익을 부정할 때

- 이유:
  - `재고·채권·이익의 질` 시리즈를 제대로 묶어준다.

### 2. DART 정정공시를 파이프라인에서 다루는 법

- 이유:
  - `031` 다음 단계로 파이프라인 운영 심화를 만들 수 있다.
  - 실제 수집기 운영에서 자주 막히는 문제를 별도 허브로 풀 수 있다.

### 3. 자기주식 취득·처분 공시 읽는 법

- 이유:
  - `corporate-actions-and-financing` 시리즈를 유상증자, CB/BW 다음 단계로 확장할 수 있다.
  - 이벤트 해석과 주주환원 해석의 중간 다리 역할을 한다.

### 4. 합병·분할 공시 읽는 법

- 이유:
  - 기업 이벤트 시리즈를 더 높은 난도의 구조 재편 공시까지 넓힐 수 있다.
  - 검색 의도와 시리즈 완성도 둘 다 강하다.

## 카테고리별 운영 방향

### 01-disclosure-systems

- 입문 허브는 `DART 첫걸음`
- 실전 확장은 `EDGAR raw filing`, `기업 이벤트·자금조달`
- 제도 설명보다 클릭 순서와 후속 확인 흐름을 우선한다

### 02-report-reading

- 첫 글은 `전체 읽기 순서`
- 다음 글은 `섹션별 실전 판단`
- 감사, 우발부채, 리스크, MD&A는 서로 교차 링크한다

### 03-financial-interpretation

- `설비투자와 고정비`와 `재고·채권·이익의 질` 두 축으로 단순화한다
- 숫자 자체보다 숫자 조합과 해석 실수를 강조한다

### 04-data-automation

- endpoint 소개보다 `수집기 설계`를 쓴다
- corp_code, rcept_no, raw filing, XBRL, notes를 레이어로 나눠 설명한다

## 제작 상태

- 작성 완료: `001` ~ `034`
- 다음 신규 우선 글: `영업현금흐름이 순이익을 부정할 때`
- 핵심 허브 리라이트: `1차 완료`
- 다음 유지 보강 우선: `입문 허브 첫 SVG 재정비`, `비어 있는 시리즈 첫 글 작성`

## 운영 메모

- 블로그의 다음 성장축은 `더 깊은 전문성`보다 `더 명확한 첫 답변`과 `빈 시리즈 채우기`다.
- 현재 병목은 짧은 글보다 `비어 있는 시리즈`와 `입문 허브 첫 SVG 설명력`이다.
- 기존 글을 강화하지 않은 채 새 글만 늘리면 시리즈는 넓어지지만 브랜드는 약해진다.

## 최근 리라이트 메모

- `004`, `007`, `008`, `009`, `013`~`030`, `021`은 2026-03-18 기준 허브 수준 리라이트를 마쳤다.
- `031`은 `corp_code -> 검색 -> rcept_no -> 원문 -> 정정 추적` 파이프라인 허브로 신규 작성했다.
- `032`는 `유상증자결정 -> 증권신고서 -> 조건 확정 -> 발행실적보고서` 흐름 허브로 신규 작성했다.
- `033`은 `CB/BW 발행결정 -> 전환/행사 조건 -> 리픽싱 -> 후속 전환` 흐름 허브로 신규 작성했다.
- `034`는 `주석 -> 투자자 보호 -> 감사보고서 -> 후속 추적` 흐름으로 우발부채와 소송 허브를 신규 작성했다.
- `001`, `002`, `003`, `005`, `006`은 `FAQ`, `체크리스트` 헤딩 정규화를 마쳤다.
- `011`은 신규 SVG를 추가했고, 현재 `짧은 글`, `FAQ 누락`, `체크리스트 누락`, `SVG 부족`, `SVG 파서 오류`, `저밀도 SVG`는 모두 `0`이다.
