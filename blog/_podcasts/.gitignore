# 발행 스크립트의 전사/빌드 임시 산출물 (오디오 mp3, feed.xml, index.json 스테이징)
_lib/.tmp/

# 오디오 미디어는 R2 런타임 산출물이라 추적하지 않는다 (용량 격리). 코드·소스·메타(guid)만 추적.
*.mp3
*.m4a
*.wav
*.aac

# _uploads: 팟빵 등 수동 업로드용 순서 아카이브 (오디오 + 16:9 커버, 영문 순번명). 결과물이라 통째 비추적.
_uploads/
