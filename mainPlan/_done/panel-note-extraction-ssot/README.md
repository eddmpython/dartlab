> 🔀 흡수·아카이브 (2026-07-06). 본 플랜(노트 한정)은 `panel-extraction-workbench-ssot` 가 **흡수·확장**했다. 그 PRD 가 정본이며, 본 문서의 결론 일부는 stale(예: `scanNotes` 를 stub 이라 기재했으나 실제는 완전 구현됨). 활성 플랜 아님. 별도 운영계약 없음(추출 능력은 `providers/dart/notes.py` + `core/_entries/notes.py` 레지스트리 소유).

# Panel 주석 추출 SSOT (흡수됨 → panel-extraction-workbench-ssot)

원본 PRD 는 [00-prd.md](00-prd.md) 에 보존(설계 근거·IFRS 32종 카탈로그·레이어 지도 자산). 정본·후속은 `../panel-extraction-workbench-ssot/` 를 본다.
