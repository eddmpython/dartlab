{
  "violations": [
    "src/dartlab/server/api/company.py::import dartlab",
    "src/dartlab/server/api/dl.py::import dartlab",
    "src/dartlab/server/services/aiAnalysis.py::from dartlab import Company"
  ],
  "_note": "module-level(col-0) stale top-level dartlab import 부채 원장. function-local lazy 는 의도된 패턴이라 제외한다. 13 건에서 3 건으로 줄였다. 사라진 둘은 애초에 오탐이었고(모듈 docstring 안 사용 예시를 정규식이 import 로 셌다) 나머지 여덟은 서브패키지 직접 import 로 바꿨다. 남은 셋 중 `server/api/dl.py` 는 capability 해석 루트라 의도적이고, `server/api/company.py` 는 facade 안에 정의된 `searchName` 을 먼저 모듈로 빼야 닫힌다. 신규 col-0 은 차단."
}
